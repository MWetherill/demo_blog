<% content_for :page_title, @post.title %>

<section class="section">
  <div class="container">
    <div class="title"><%= @post.title %></div>
    <div class="content">
      <%= @post.content %>
    </div>
  </div>
</section>